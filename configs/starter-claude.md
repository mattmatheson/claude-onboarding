# Project Instructions

## About Me
- Name: [YOUR NAME]
- Focus: [What you're building — dashboards, websites, automation, etc.]
- Stack: [Your preferred tech — e.g., Next.js, React, Tailwind, Vercel]
- Experience: [Your level — beginner, intermediate, comfortable with CLI, etc.]

## Code Preferences
- TypeScript over JavaScript
- Tailwind CSS for styling
- Use App Router (not Pages Router) for Next.js
- Prefer server components unless client interactivity is needed
- Write clear commit messages that explain WHY, not just what
- Always check for existing files before creating new ones

## GitHub
- Use the `gh` CLI for all GitHub operations (PRs, issues, repo management)
- Always create feature branches for new work (never commit directly to main)
- Write descriptive PR titles and summaries

## Safety Rules
- NEVER hardcode API keys, tokens, or secrets in code
- Use .env files for all secrets, and ensure .env is in .gitignore
- For sensitive keys, use macOS Keychain: `security add-generic-password`
- Never force-push to main
- Never run `rm -rf` without asking first
- Never commit .env files or credentials
- Run `npm audit` periodically to check for vulnerabilities

## Obsidian Vault
- My knowledge vault is at ~/Vault/
- Check it for context before asking me questions
- Use [[double brackets]] for internal links
- Write session summaries to daily/ after significant work sessions

## Communication
- Be concise — skip the preamble
- Don't ask unnecessary questions — check the code/vault first
- If something is unclear, make your best judgment and explain what you assumed
- Explain things when I'm learning something new
